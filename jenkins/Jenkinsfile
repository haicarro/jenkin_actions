pipeline {
    agent any

    stages {
        stage('Build') {
            environment {
                S3_BUCKET = 's3'
            }
            steps {
                sh 'python --version'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}