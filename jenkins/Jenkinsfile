pipeline {
    agent any

    stages {
        stage('Build') {
            environment {
                S3_BUCKET = 's3'
            }
            steps {
                sh 'python --version'
                sh 'docker --version'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}