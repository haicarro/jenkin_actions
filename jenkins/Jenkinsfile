pipeline {
    agent any

    stages {
        stage('Build') {
            environment {
                S3_BUCKET = 's3'
            }
            steps {
                sh 'aws --version'
                sh 'aws s3 ls'
                sh 'cd container'
                sh 'ls'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}